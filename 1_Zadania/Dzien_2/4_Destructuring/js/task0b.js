const slider = {
    type: "infinite",
    numberOfItems: 10,
    center: true,
    autoStart: true
};

let {type, autoStart} = slider;
console.log('type', type);
console.log('autoStart', autoStart);
